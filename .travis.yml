sudo: false
language: go
git:
    depth: 1
install: 
    - rm -rf public || exit 0
script:
    - ./binaries/hugo --theme=beautifulhugo
deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: master
    local_dir: public
    repo: whalecold/whalecold.github.io
    #fqdn: <custom-domain-if-needed>
    target_branch: master
    email: zls1129@gmmail.com
    name: whalecold
