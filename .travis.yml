sudo: false
language: go
git:
    depth: 1
install: 
    - rm -rf public || exit 0
branches:
  only:
    - blog
script:
    - ./binaries/hugo --theme=pure
deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: blog
    local_dir: public
    repo: whalecold/whalecold.github.io
    #fqdn: <custom-domain-if-needed>
    target_branch: master
    email: $GITHUB_EMAIL
    name: whalecold
